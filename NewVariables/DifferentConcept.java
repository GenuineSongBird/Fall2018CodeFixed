package NewVariables;

import java.util.Random;

public class DifferentConcept {
	//Math class - built in methods
	System.out.println(Math.ceil(3.4));
	System.out.println(Math.floow(3.9));
	
	System.out.println(Math.sqrt(4));
	System.out.println(Math.max(5,7));
	
	System.out.println(Math.min(4,2));
	Random r = new Random();
	//r.nextInt(10);
	System.out.println(Math.random()); //0 and 1
	Math.round(Math.random())* 5 + 1; //finds a integer (1 2 3...) between 1 and 6
	//Math.abs(-2)
	
		//Wrapper Classes
		//integer
		//double
	
		 int s  = Integer.parseInt("5") ;  //turns 5 from a string to a integer
		 int v = (int)(Double.parseDouble("5.5")); //turns 5.5 from a string to a double to a int
		
}
